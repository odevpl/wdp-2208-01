/* selectors */
export const getPromotedDeals = ({ promotedDeals }) => promotedDeals;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
